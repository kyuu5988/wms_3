<div>
    <div class="main_temp">
      <%= render "main" %>
    </div>
    <h2>検索結果（無入力時 全データ表示）</h2>
    <%= notice %>
    <p class="p02">"<%= @keyword %>"で検索しています。</p>


    <div class="search-list">
      <% count = 0 %>
      <% @samples.each do |sample| %>
        <% count += 1 %>
        
        <p class="sea-res">

          <%= sample.基本コード %>-
          <%= sample.サブコード %> #
          <%= sample.個別番号 %>　　　ロケ：
          <%= sample.ロケーション %>

        </p>

        <p class="sea-res2">
          <%= sample.販売商品名 %>　　　
          <%= sample.カラー %>　　　
          <%= link_to '情報変更', edit_sample_path(sample.id),method: :get,class: 'btn01' %>
        </p>

      <% end %>
        <p><%= count %>件見つかりました。</p>
          

    

</div>