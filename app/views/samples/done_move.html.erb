<p class="fin"><%= flash[:already] %>
<p class="p03">*スタジオ移動専用のタグをスキャンすると<br>同じ申込番号のサンプルの別のサブ（別カラー）をまとめて移動します。<br>実際の現場でもOA前の準備ではこのように移動します。</p>

<div class="search-list">
  <% @products.each do |prod| %>
    <p class="sea-res">
    <%= prod.基本コード %>-
    <%= prod.サブコード %> #
    <%= prod.個別番号 %>　　　ロケ：
    <%= prod.ロケーション %>

    </p>

    <p class="sea-res2">
    <%= link_to "#{prod.販売商品名}", sample_path(prod.id),method: :get, class:'sample-btn' %>
    　　　<%= prod.カラー %>
    </p>

    <% if prod.rent != "(該当無し)" &&
          prod.rent != "" %>
      <p class="fin">　貸出中：<%= prod.rent %></p>
    <% end %>


  <% end %>
</div>